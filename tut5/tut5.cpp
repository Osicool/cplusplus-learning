#include <iostream>

using namespace std; //using directive

int main(){
    cout << "This is using the directive so no namespace!\n";
    std::cout << "However, using the namespace is better because it means your really specific and may avoid naming conflicts!\n";
    return 0;
}