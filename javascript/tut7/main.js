(function(){
    //IIFE - immediately invoked function expression
    var age = 24; //Variable age exists within this scope
    pizza = 24; //Variable pizza will exist beyond this scope (it's global)
})();